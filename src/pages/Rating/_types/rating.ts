export type Rating = Array<RatingItem>;

export type RatingItem = {
  user: {
    id: string,
    profile: {
      platform: string,
      id: 126399522,
      firstName: string,
      lastName: string,
      photoUrl: string
    }
  },
  record: number
};